## Updated prompt (with Zod validators + inferred TypeScript types)

1. Create MongoDB collections for `regions` and `countries` (and define their schemas/structure).
2. Populate the Region and Country dropdowns using data read from these collections (no hardcoded lists in the UI).
3. During application startup (or the first successful DB connection), perform a bootstrap/seed check:

   * If the `regions` or `countries` collection does not exist **or** exists but contains no documents, insert the initial predefined values.
   * Use the same initial values currently hardcoded in the existing codebase (do not invent new ones).
4. Read MongoDB connection parameters from `.env.local`.

   * Use `dotenv-flow` to load environment variables (ensure `.env.local` is supported in the loading order).
   * Do not commit secrets; document required env keys in `README.md`.
5. Create Zod schemas (validators) for `Region` and `Country` documents (and any DTOs you use, e.g., “create region/country” payloads).

   * Infer TypeScript types from these Zod schemas using `z.infer<typeof Schema>`.
   * Use the inferred types throughout the codebase (DB access layer, API handlers, UI data fetching) to ensure type safety.
   * Validate data at boundaries:

     * When reading from MongoDB (parse/validate documents into typed objects).
     * When accepting any input payloads (if you add APIs for CRUD, validate request bodies with Zod).

### Optional clarifications (recommended to prevent ambiguity)

* Run seed/bootstrapping logic **server-side only** (not in client components).
* Make seeding **idempotent** (safe to run repeatedly without duplicating records).
* Add unique indexes to enforce idempotency:

  * `regions.code` unique
  * `countries.iso2` (or `countries.code`) unique
* Ensure `countries.regionId` references a valid `regions._id` (or store `regionCode` if you prefer avoiding cross-collection ObjectId coupling for seed data).
